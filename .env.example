# Frontend — base URL of the FastAPI backend.
# For local dev with `uvicorn main:app --reload` on port 8000, leave as-is.
# For hosting, set this to your deployed backend URL, e.g. https://api.example.com
VITE_API_BASE_URL=http://localhost:8000
